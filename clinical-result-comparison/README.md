# Clinical Result Comparison

Tool Smith project assets for source-grounded synthesis and comparison of multiple selected clinical result records. The default output is a trial-level evidence synthesis for same-trial selections and a comparison-first, domain-aligned cross-trial comparison for mixed selections, each with a compatible chart when the data allow it.

## Product boundary

- Input is delivered as a **selected esid list**: the frontend sends each selected clinical result as its clinical-result esid (`clinical_result.extra_esid`) in the user turn. The Agent pulls the detail records itself through the MCP tool `pharmcube-query-clinical-result-with-params`, passing `extra_esids` plus a strict, minimal `selected_fields` allowlist (names copied verbatim from ALLOWED_FIELD_NAMES in `docs/params-tool-schema.md`). Clinical evidence comes from the pulled clinical-content fields (`abstract_text`, `summary`, `study_results`, design/arms context); `paper_title`, `full_article_link`, `paper_release_time` are citation metadata; entity fields (`projects.associate_ids`, `trial_abbreviation`, `arms[].drugs[].drug_earth_id`, `projects.company_ids`) are display/label metadata for inline entity references only.
- The backend/frontend supply only the selected esids; the Agent never fabricates, guesses, or retrieves URLs, IDs, or records outside the pulled set. Runtime identifiers and retrieval/storage details remain outside the Agent request and report.
- Per-esid mapping keeps large selections (20–50 results) addressable; a strict `selected_fields` set bounds the returned payload (a full record or a huge response can exceed the context budget). Input (esid) order defines `{{ref_n}}` assignment. Input order is a presentation label, not clinical chronology.
- Runtime identifiers and retrieval/storage details remain outside the Agent request and report.
- The primary analysis object is the underlying trial. Multiple disclosures from one trial are source nodes that are deduplicated into a longitudinal evidence chain covering design, efficacy, safety, maturity, and information gaps.
- Every material number and source-dependent conclusion carries an internal citation token for rendering; the user-facing report shows numeric superscripts. Citation metadata is emitted separately as strict JSON, with `title`, `link`, and `paper_release_time_str` per ref.
- For mixed inputs containing different trials, the deliverable is comparison-first and domain-aligned: efficacy, safety, PK/PD, and PRO are aligned across trials so the user can judge which treatment is better or worse, with explicit comparability/evidence-strength labels and compact per-trial context. The report does not pool results or fabricate head-to-head proof. `{{ref_n}}` numbering is global across the complete response and the separate citation JSON is shared; numbering resets only when the runtime explicitly requests separate independent reports.
- Version 1 uses only selected results and performs no external retrieval.
- The frontend may render each marker as a numeric superscript link using the final JSON. It must preserve exact supplied URLs and use safe external-link attributes.
- The backend/frontend enforce a conservative real-time token budget.

## Files

- `system-prompts/multi-clinical-result-comparison-v0.15.md`: current Tool Smith prompt — the v0.14 prompt plus the HTML cleanup: identical apart from the version label and two generalisations of the vestigial v0.12-debug `.preview.html` guard into "never a non-`.json` chart file".
- `system-prompts/multi-clinical-result-comparison-v0.14.md`: previous prompt — v0.12 full-synthesis workflow with the chart protocol delegated to the `chart-visualization-json` skill, no TEMP `.preview.html` dual-write, and chart validation through that skill's CLI.
- `system-prompts/multi-clinical-result-comparison-v0.13.md`: snapshot of the abandoned lightweight quick-comparison variant (kept for reference, not shipped).
- `system-prompts/multi-clinical-result-comparison-v0.12.md`: historical full-synthesis prompt carrying the TEMP chart preview contract.
- `skill/multi-clinical-result-comparison/`: runtime Skill, evidence-chain references, citation contract, and report template.
- `skill/multi-clinical-result-comparison/templates/unified-evidence-report.md`: single-trial consumer-facing report structure.
- `skill/multi-clinical-result-comparison/references/timeline-diagram.md`: construction rules for the evidence-chain timeline as a pure-JSON timeline diagram (same-trial, ≥2 distinct evidence states) built on `templates/charts/evidence-timeline.json` with `::visualization` references; no Mermaid.
- `skill/multi-clinical-result-comparison/templates/cross-trial-report.md`: comparison-first, domain-aligned report structure for multiple independent trials (efficacy, safety, PK/PD, PRO).
- `skill/multi-clinical-result-comparison/references/file-delivery.md`: v0.9 file-delivery contract with **fixed deliverable paths** — final report written to `/workspace/output/report.md`, citation JSON to `/workspace/output/citations.json`, delivered with `present_artifact` as the terminal tool call, empty/minimal chat body.
- `skill/multi-clinical-result-comparison/references/citation-and-ref.md`: inline marker, metadata, and separate JSON citation contract.
- `skill/multi-clinical-result-comparison/references/entity-inline-reference.md`: v0.10–v0.12 entity inline reference contract — `[name](entity:type:id)` links for 药品/公司/临床试验注册号, with IDs only from the pulled record's entity fields (`projects.associate_ids`, `trial_abbreviation`, `arms[].drugs[].drug_earth_id`, `projects.company_ids`); the trial display label prefers the `trial_abbreviation` short name (`[HARMONi-6](entity:trial:NCT05840016)`), falling back to the registration id. No fabricated IDs, no entity refs inside chart files.
- `skill/multi-clinical-result-comparison/references/chart-templates.md` + `templates/charts/`: pure-JSON clinical starting points (evidence-timeline / endpoint-bar / endpoint-line `.json`). Protocol, skeleton templates, Zod schemas and validation are delegated to the upstream **`chart-visualization-json`** skill (`node /workspace/skills/chart-visualization-json/scripts/validate-cli.js <product.json>`); the in-house `validate-chart.py` is no longer shipped. **v0.15 deleted every HTML chart asset from the repository**: `docs/legacy-html-charts/` (the v0.8–v0.11 HTML chart sources plus the retired `validate-chart.py` and `render-preview.py`) no longer exists, so the repository ships no `.html`, `.css` or chart-related `.py` file at all — recover it from git history at `e4f3bb7` if ever needed.
- `docs/params-tool-schema.md`: archived authoritative schema of `pharmcube-query-clinical-result-with-params` (ALLOWED_FIELD_NAMES + nested shapes); the source of truth for `selected_fields`.
- `evals/fetch-np-clinical-attachments.mjs` (historical, superseded in v0.12): the v0.7–v0.11 reproducible adapter that wrote one `.md` attachment file per source plus a backend `manifest.json`, enriched with `source_nct_id`, `source_drug_entities`, `source_company_entities`, and (v0.11) `source_trial_abbr` lines.
- `evals/fetch-np-clinical-by-nct.mjs`: read-only `base.nct_id` adapter for producing normalized source objects.
- `evals/fetch-np-clinical-by-condition.mjs`: original condition-query adapter for the supplied indication/phase/evaluation/NCT/deletion/featured filter.
- `evals/fixtures/np-clinical-indications-516-517-phase-featured-false.json`: 10-source normalized example generated from the requested query; the query matched 12 records and returned 10 usable full-text sources.
- `evals/fetch-np-clinical-sources.mjs`: read-only selected-ID reference adapter using the POC environment configuration.
- `runtime/citation-renderer.mjs` and `test/citation-renderer.test.mjs`: accept the Markdown report and separate citation JSON, validate marker/key parity, and render only numeric superscripts from valid source links.
- `evals/validate-v05-contract.mjs`: local contract check for normalized fixtures and linked citation output.
- `evals/fixtures/harmoni6-selected-results.json`: same-trial multi-disclosure fixture for trial-chain regression.
- `evals/fixtures/nct05840016-selected-results.json`: live `np_clinical` example fetched by `base.nct_id = NCT05840016`, containing four HARMONi-6 source objects.
- `evals/iteration-11-harmoni6-trial-synthesis.md`: v0.4 hand-authored trial-level regression report with inline Refs.
- `evals/iteration-12/harmoni6/report-v2.md`: local Skill execution regression report; the v2 rerun verifies proper-name fidelity.
- `dist/multi-clinical-result-comparison-v0.15.zip`: current Skill archive (v0.14 content with the HTML chart assets removed from the repository; system prompt kept outside the archive).
- `dist/multi-clinical-result-comparison-v0.14.zip`: previous Skill archive (esid + params-tool input, pure-JSON chart protocol delegated to the `chart-visualization-json` skill, file-based report delivery, entity inline references with trial short-name display; system prompt kept outside the archive).
- `dist/multi-clinical-result-comparison-v0.13.zip`: archive of the abandoned lightweight quick-comparison variant (system prompt kept outside the archive).
- `dist/multi-clinical-result-comparison-v0.12.zip`: historical v0.12 Skill archive (esid + params-tool input, pure-JSON chart protocol, file-based report delivery, entity inline references with trial short-name display). `dist/multi-clinical-result-comparison-v0.11.zip` and older remain historical archives.
- `dist/multi-clinical-result-comparison-v0.12-temp-preview.zip`: **deleted in v0.15** (user request). It was the superseded TEMP debug build that bundled `templates/charts/render-preview.py` to mechanically dual-write a `<name>.preview.html` so charts stayed visible in the chat page before the renderer landed; v0.14 removed the dual-write and v0.15 removed every HTML asset, so the archive was dropped. It was never committed and is not recoverable.

## v0.15 changes

- **The HTML chart pipeline is gone from the repository.** `docs/legacy-html-charts/` — the v0.8–v0.11 HTML chart templates (`endpoint-bar.html`, `endpoint-line.html`, `evidence-timeline.html`), `chart-tokens.css`, the retired in-house validator `validate-chart.py` and the v0.12 preview renderer `render-preview.py` — is deleted, so no `.html`, `.css` or chart-related `.py` asset remains anywhere in the repo. Nothing was ever shipped from that directory (the v0.14 bundle never contained it), so **the runtime contract is unchanged**; recover the files from git history at `e4f3bb7` if needed.
- **Left in place on purpose: the guards that stop HTML from coming back.** `references/chart-templates.md` still states that the Skill emits no HTML/SVG fragment and no Mermaid, writes no `.html`, and never mixes a custom palette or CSS/HTML token into the JSON; the system prompt still forbids an HTML/SVG chart fragment or any non-`.json` chart file; final verification still asserts that every `::visualization` target is a `.json` file under `/workspace/visualizations/`. Only the two vestigial `.preview.html` mentions (a v0.12-debug artifact) were generalised.
- **Eval HTML chart outputs removed as well.** The 7 tracked `evals/iteration-16/chart-examples/*.html` files are deleted, and the 14 run-scoped HTML charts under the ignored `evals/iteration-16/np-clinical-*/` directories were moved out of the working tree, so **no `.html` or `.css` file remains anywhere in the repo** (working tree checked). The rendered `.png` previews stay as historical screenshots and `chart-examples/README.md` was rewritten to describe them as HTML-era previews. The tracked originals are recoverable from git history at `e4f3bb7`.
- `references/chart-templates.md` no longer points at the archived HTML directory.
- `system-prompts/multi-clinical-result-comparison-v0.15.md` replaces v0.14 as the current prompt; v0.14 and earlier stay as snapshots.

## v0.14 changes

- **Deliverable file names are now fixed, so the backend can hard-code them.** The previous `<slug>`-based naming (descriptive prefix chosen by the model) is gone: the report is always `/workspace/output/report.md`, the citation JSON is always `/workspace/output/citations.json`, and chart products are always `/workspace/visualizations/evidence-timeline.json` plus `/workspace/visualizations/endpoint-bar-<n>.json` / `/workspace/visualizations/endpoint-line-<n>.json` (`n` = that kind's order in the report, from 1, so a lone quantitative chart is always `endpoint-bar-1.json` or `endpoint-line-1.json`). The chart kind is in the file name **and** must match the JSON `type` field, because the renderer dispatches on `type` (`bar` / `line` / `timeline`). The backend no longer reads the path back from the `present_artifact` result. Reruns overwrite these files in place — one current report per workspace, matching the platform rule that a presented path means "the current version of this path". Trade-off: the artifact card and the download name become `report.md` (no `display_name` parameter today) and earlier reports in the same workspace are overwritten.
- **v0.13 abandoned; v0.14 is built on the v0.12 baseline.** The lightweight quick-comparison variant is kept on disk for reference (`docs/legacy-v0.13/`, `dist/multi-clinical-result-comparison-v0.13.zip`) but is no longer the shipped Skill: the shipped bundle returns to the v0.12 full-synthesis assets and report templates.
- **TEMP dual-write removed; `::visualization` references point back at `.json`.** The renderer is being integrated, so `references/chart-templates.md`, `references/timeline-diagram.md`, `SKILL.md`, the report templates and the system prompt no longer emit or mention a `.preview.html` twin; `templates/charts/render-preview.py` moved to `docs/legacy-html-charts/` (that archive directory was itself deleted in v0.15).
- **Chart protocol, templates and validation now reuse the upstream `chart-visualization-json` skill** instead of a parallel in-house implementation. `references/chart-templates.md` was rewritten to delegate the protocol, skeleton templates, Zod schemas and validation to that skill; the local `templates/charts/*.json` files are now only clinical pre-filled starting points. The in-house `python3 templates/charts/validate-chart.py` is no longer shipped (archived under `docs/legacy-html-charts/`, deleted in v0.15) and validation runs through the chart skill CLI: `node /workspace/skills/chart-visualization-json/scripts/validate-cli.js <product.json>` (frontend alias `pnpm validate:chart -- <path>`).
- **Chart validation fails loud.** If the chart skill or its `zod` dependency is missing at runtime, the run must state that the chart-validation environment is unavailable, keep the exact-value table as the primary deliverable, and emit no unvalidated `::visualization` reference — never skip validation silently and never fall back to a local validator.
- **Side-by-side bars disable stacking explicitly.** `templates/charts/endpoint-bar.json` now sets `group: false, stack: false`, because the protocol defaults `stack: true`.
- `system-prompts/multi-clinical-result-comparison-v0.14.md` replaces v0.12 as the current prompt; v0.13 stays as a snapshot of the abandoned variant.

## v0.13 changes (abandoned, kept for reference)

- **Default workflow simplified for speed.** The Agent now makes one MCP pull and one compact digest, chooses one primary comparison question and one compatible endpoint, writes one compact alignment table, and generates at most one quantitative chart. When all usable selected records belong to one trial and contain multiple distinct disclosures, it also generates the compact evidence-chain timeline. Mixed-trial inputs never receive the default timeline. Endpoint-family scans, multiple quantitative charts, and long subgroup reviews remain opt-in deep-review behavior.
- **Default report shortened.** The new `quick-comparison-report.md` contains a one-sentence takeaway, core alignment table, a same-trial timeline section only when all usable records belong to one trial and multiple states exist, one quantitative visualization slot, short interpretation/boundaries, and up to three decision-relevant gaps.
- **Rigor retained as invisible constraints.** Selected-field minimization, source markers, citation parity, duplicate handling, no value inference/pooling, safety context, entity-ID provenance, chart validation, and `present_artifact` delivery remain mandatory.
- **Chart policy changed to one quantitative chart plus the all-records-single-trial timeline when applicable.** Compatible cross-sectional values use one bar chart; same-study/cohort/endpoint time points use one line chart; two or more distinct evidence states are shown on a timeline only when every usable selected record belongs to the same trial. Mixed-trial inputs do not get the timeline.


- **Input contract switched from `.md` attachments to esid + params tool.** The user turn now carries each selected clinical result as its esid (`clinical_result.extra_esid`); the Skill pulls detail records through MCP `pharmcube-query-clinical-result-with-params` (`extra_esids` + strict `selected_fields`). `references/input-contract.md` rewritten as the esid/params delivery contract (pull protocol, recommended `selected_fields`, response limits, consumer-field mapping table `source_*` → `paper_title`/`full_article_link`/`paper_release_time`/`projects.associate_ids`/`trial_abbreviation`/`arms[].drugs[].drug_earth_id`/`projects.company_ids`). `references/citation-and-ref.md`, `references/input-and-extraction.md`, `references/file-delivery.md`, `references/entity-inline-reference.md`, `SKILL.md`, the three report templates, and `system-prompts/multi-clinical-result-comparison-v0.12.md` updated; the v0.11 `source_full_text` single-full-text evidence model is replaced by the pulled `abstract_text`/`summary`/`study_results` clinical-content fields.
- **Charts moved from HTML fragments to the chart-visualization-json JSON protocol.** Chart products are pure JSON (only `line`/`bar`/`timeline` renderable), filled from skeletons `templates/charts/endpoint-bar.json` / `endpoint-line.json` / `evidence-timeline.json` and validated by the in-house `python3 templates/charts/validate-chart.py` (valid JSON, allowed type, meta field types, row shapes, timeline legend/weight, pure-number values, ≤1 MiB). `references/chart-templates.md` and `references/timeline-diagram.md` rewritten to the JSON protocol; the v0.8–v0.11 HTML templates were archived to `docs/legacy-html-charts/` (deleted in v0.15). Text in charts is limited to the protocol's `description`/`describe`/`subTitle`/`dataSource`; in-chart refs, CI/error bars and interactive tabs are dropped, and exact values/boundaries stay in the body tables.
- **Sorting is the generator's responsibility**: the renderer does not reorder, so the Agent pre-sorts `data[]` (bars high→low by value; line/timeline chronological).
- `system-prompts/multi-clinical-result-comparison-v0.12.md` replaces v0.11 as the current prompt; v0.11 and earlier remain as snapshots.

## v0.11 changes

- Trial short name inline reference: the entity-ID metadata now includes `source_trial_abbr` (from `base.trial_abbreviation`), so the report can display the trial by its short name (e.g. `[HARMONi-6](entity:trial:NCT05840016)`, `[TRAIN-2](entity:trial:NCT01996267)`) instead of only the registration number. The `entity:trial:` ID stays the registration number (`source_nct_id`); the short name is display-only and falls back to the registration id when the abbreviation line is absent.
- Fetch adapter pulled `base.trial_abbreviation` and writes `source_trial_abbr`; `input-contract.md`, `entity-inline-reference.md`, `SKILL.md`, the three report templates, and `system-prompts/multi-clinical-result-comparison-v0.11.md` updated accordingly.
- `evals/iteration-16` data dirs re-pulled with the new metadata line (nsclc-50 34/50, ind579-phase3-50 40/59, nct01996267 5/5 have a short name); all generated artifacts, gitignored.

## v0.10 changes

- **Entity inline references** (药品 / 公司 / 临床试验注册号). When an attachment file carries the new entity-ID metadata lines, the Agent renders every mention of those drugs, companies, and the registration number as Tool Smith entity links `[name](entity:drug:id)` / `[name](entity:company:id)` / `[name](entity:trial:NCT…)` so the frontend `EntityAnchor` shows clickable entity tags. The artifact reader (`ArtifactMarkdownViewer` → `ChatMarkdown` → `EntityAnchor`) already renders these, so the frontend needs no changes.
- **ID source = backend metadata passthrough**: new `references/entity-inline-reference.md`; `references/input-contract.md` adds the `source_nct_id` / `source_drug_entities` / `source_company_entities` lines (metadata-line priority for nct_id; no text-extraction fallback). The Agent never fabricates IDs and never queries tools for IDs.
- `evals/fetch-np-clinical-attachments.mjs` enriches each attachment with the entity lines: `base.nct_id` → `source_nct_id` (first clean token, NCT preferred), `base.trial_drug` meta → `source_drug_entities` (`名称|drug_earth|ID`), and `trial_details` `company_ids` → `source_company_entities` via a `base_company` `terms`-on-`id` resolution (`name_show_cn` → `short_name` → `name`). Graceful degradation: a source with no IDs simply omits the line.
- Entity metadata lines are **display/label metadata, not clinical evidence**; entity refs coexist with `{{ref_n}}` and never appear inside `::visualization` chart files.
- Scope is limited to drug + company + trial registration number; indication/target remain disabled (no metadata lines).
- New system prompt `system-prompts/multi-clinical-result-comparison-v0.10.md`; historical v0.3–v0.9 prompts remain as snapshots.

## v0.9 changes

- **File-based report delivery via `present_artifact`** (方案 B). The finished report is no longer streamed as chat text. It is written to `/workspace/output/<slug>-report.md` (report template content with inline `{{ref_n}}` markers and `::visualization` references) and the strict citation JSON to `/workspace/output/<slug>-citations.json`, then the report file is delivered with `present_artifact` as the **final tool call**. The chat body stays empty or holds at most one short purpose line — no intro text, no report content, and no tool call after `present_artifact`.
- Backend no longer parses model output: it serves the report and citation files directly from the artifacts API; the citation file is a visible workspace artifact even without a card.
- `{{ref_n}}` markers and the separate citation JSON schema are unchanged; downstream consumers render markers using the citation file. New `references/file-delivery.md` is the authoritative delivery contract.
- New system prompt `system-prompts/multi-clinical-result-comparison-v0.9.md`; historical v0.3–v0.8 prompts remain as snapshots.
- `runtime/citation-renderer.mjs` adds `validateCitationPair(report, citationJson)` (parity check without rendering) with tests.

## v0.8 changes

- All charts (same-trial evidence timeline and cross-trial bar/line) moved from Mermaid to **HTML templates + `::visualization` references**. `references/timeline-diagram.md` rewritten to build the timeline from `templates/charts/evidence-timeline.html` (one event per evidence state, merging duplicate disclosures); `references/cross-trial-comparison.md` chart contract now uses `endpoint-bar.html`/`endpoint-line.html`; templates `unified-evidence-report.md`, `mixed-comparison-report.md`, `cross-trial-report.md`, `SKILL.md`, and `system-prompts/multi-clinical-result-comparison-v0.8.md` updated. Cross-trial/mixed reports grouped by clinical question produce **one bar chart per group** (each group its own `endpoint-bar.html` product file; experimental-arm values side by side; comparators and boundaries in hover notes; 跨试验并列展示≠头对头比较 stated). No Mermaid output anywhere.
- References use **absolute paths** per the Tool Smith visualization contract: files are written to `/workspace/visualizations/` first, then `::visualization[标题]{path="/workspace/visualizations/xxx.html"}` on its own line (no relative path, no `..`/backslash/out-of-prefix, ASCII filename, no subdirectory, ≤1 MiB for HTML). Charts are **HTML fragments** (no `<!doctype html>`/`<html>`/`<head>`/`<body>` document wrapper — Tool Smith injects them with the widget fragment renderer; it validates by substring match over the whole file, so `<head`-prefixed tags like `<header>` must be avoided, templates use `<div class="header">`). The full delivery-path contract lives in `references/chart-templates.md`.
- New system prompt `system-prompts/multi-clinical-result-comparison-v0.8.md`; historical v0.3–v0.7 prompts remain as snapshots.

## v0.7 changes

- Input switched from inline JSON in a single message to **one attached `.md` file per selected result** (attachment-based delivery). Each file carries the four consumer fields (metadata lines + full text under `## source_full_text`); the Agent reads every attached file in filename order and assigns `{{ref_n}}` markers in that order. This keeps large selections (20–50 results) within the context budget.
- `references/input-contract.md` rewritten: per-source file layout, attachment limits (10 per message / 100 per thread / 20 MiB per file), Agent reading protocol, and backend manifest. `SKILL.md` and `system-prompts/multi-clinical-result-comparison-v0.7.md` updated accordingly.
- `evals/fetch-np-clinical-attachments.mjs`: pulls `np_clinical` by condition (default: non-small-cell lung cancer 135/5718/5719 + phase 3 + positive evaluation, ~890 matched) and writes one attachment file per source plus `manifest.json`, with a built-in round-trip check.

## v0.6 changes

- Same-trial inputs with two or more distinct evidence states now output a descriptive Mermaid evidence-chain timeline diagram directly above the timeline table in the 证据链总览与时间线 section, per `references/timeline-diagram.md`: one node per evidence state with key labels and `{{ref_n}}` markers, a source-supported time axis (时间未明 when missing), dotted time-to-state links, and a labeled relationship + maturity-direction arrow between consecutive states.
- The diagram is descriptive/chronological and does not require numeric compatibility; it never replaces the exact-value timeline table. Quantitative charts remain restricted. If only one distinct state remains or order cannot be determined, no diagram is generated and the order uncertainty is stated in the table.

## Tool Smith configuration

Use the v0.14 system prompt and the v0.14 Skill archive. Charts are validated with the upstream `chart-visualization-json` skill CLI; if that skill (or its `zod` dependency) is unavailable in the workspace, the run must report the validation environment as unavailable instead of emitting unvalidated chart references. The project must enable the **`artifact_presentation`** capability and keep **`visualization`** enabled. The backend/frontend supply only selected esids; the Agent pulls details through MCP with strict `selected_fields`.

The product and backend own token counting and request rejection before the Agent run. The Agent must never silently truncate accepted source text.
