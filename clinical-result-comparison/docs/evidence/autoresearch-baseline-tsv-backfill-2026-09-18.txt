# autoresearch baseline TSV backfill (zero new runs) — 2026-09-18
#
# command (one row per run; --score runs evals/fact-check/check.py offline):
#   tools/replay-run.py --tsv --score --commit <sha> --status <keep|discard|baseline> \
#       --description '<what changed>' <run-dir>
# header + column semantics: tools/replay-run.py --help

20260914-171953-cite-date	b3bd57b	e6d9c5fd4188	b?	16	21	129.0	8	8	0	49064	1	3	12	baseline	pre-harness: citation-date experiment (params=8)
20260914-173645-a-2valid	b3bd57b	b3050f32668e	a	18	23	175.3	2	8	2	69877	1	38	41	baseline	scenario A, v1.5-era prompt
20260914-173947-b-refusal	b3bd57b	b3050f32668e	b	8	8	36.3	3	3	0	9439	1	12	12	baseline	scenario B refusal (v1.5-era)
20260914-174145-b2-refusal	b3bd57b	1998f37da67d	b	7	8	33.5	2	4	0	9249	1	12	12	baseline	scenario B refusal (b2)
20260914-174222-a2-2valid	b3bd57b	1998f37da67d	a	34	41	209.5	2	18	6	70293	1	40	41	baseline	scenario A, 6 hardening items landed
20260918-095553-r17-sign-convention	b3bd57b	ac0b7e71c2db	a	1	27	186.2	1	9	1	60782	1	41	41	baseline	scenario A, sign-convention round
20260918-110804-r18-abstract-size	b3bd57b	4a1bdd5b37ba	a	1	32	204.8	3	11	5	72130	1	41	41	keep	scenario A, B1 abstract-size wording

# Reading (what the backfill answers, and what it does not)
#
# 1. The §6 table's own open question — "cost went up (23 -> 41 calls), did quality?" — is now
#    answerable offline: 38/41 -> 40/41 facts for +18 calls. The new numbers are NOT a single
#    score: cost and facts are read side by side, and a fact-score gain bought with calls is a
#    trade, not a win (§9).
# 2. R17 (27 calls, 41/41) vs R18 (32 calls, 41/41): B1 raised cost with no fact-score change.
#    n=1 per arm, and the B1 wording only touches the *source-class* path — do NOT call this a
#    regression on one sample (§8.2: scenario A needs 3 repetitions for a noise band).
# 3. Scenario B is a genuinely cheap gate: 8/8/8 calls, 33-36 s, 12/12 facts across three
#    prompts of two eras. Any refusal-path change should be judged against these three rows.
# 4. `turns` semantics changed on 2026-09-15 (platform `/info` stats.turns = 1 for a whole turn;
#    the old harness counted client model turns). Rows before that date carry the old count;
#    `replay-run.py --details` prints `model_turns` for every row so both eras stay comparable.
# 5. `calls` / `params_calls` count *attempts* (call events), not returned calls: R18 is 32 attempts
#    = 30 returned + 2 schema-rejected (params x1, web_fetch x1). Same convention as the
#    `tool calls:` line in verification.md, so the two never disagree by accident.
# 6. `deployed_sys_sha` is the sha256[:12] of the *assembled* instructions (our prompt + platform
#    tail), which is why it is stable per deployment and why it never equals a local file hash.
#    `--details` additionally reports which local prompt file is a byte-prefix of it.
# 7. `deployed_sys_sha` is only comparable between runs with the SAME web setting. The assembled
#    instructions contain the platform's `## Web Tools` block when `enable_web` is on, so R18
#    (--web) sha 4a1bdd5b37ba = 71,481 B while R19/R20 (no web) sha e11809235f47 = 70,827 B;
#    the 654-byte delta is exactly that block (`diff` = one hunk `@@ -364,8 +364,2 @@`). The bit
#    is not random per run — it is the deployment + the web flag. `--details` now prints the
#    `enable_web` marker next to it.
# 8. `calls` / `params_calls` count attempts, and an attempt that produced no tool-return is split
#    into two buckets instead of one (runner O32, 2026-09-18):
#      * `args-refused`      the arguments were refused by the tool (shape/name/value) — the
#                            framework re-prompted and the model re-issued the call
#      * `fetch-failed`      an external retrieval failed (HTTP 4xx/5xx, unreachable host)
#    Before O32 both were printed as "schema-rejected" and — worse — the fetch failures were
#    hidden from the `errors` list, so the FAIL-level `no tool errors` assertion stayed green
#    while a fetch had actually failed. Per-run buckets as re-read offline (zero network):
#      R15 (30 att)  0 args-refused | 1 fetch-failed  web_fetch europepmc fullTextXML 500
#      R16 (30 att)  1 args-refused | 1 fetch-failed  + `bash` unknown tool name
#      R17 (27 att)  0 args-refused | 1 fetch-failed  web_fetch europepmc fullTextXML 500
#      R18 (32 att)  1 args-refused | 1 fetch-failed  params INVALID_INPUT `clinical_result.moa`
#      R19 ( 7 att)  1 args-refused | 0 fetch-failed  params `extra_esids` (arg is `esids`)
#      R20 ( 8 att)  1 args-refused | 0 fetch-failed  same
#    `https://www.ebi.ac.uk/europepmc/webservices/rest/PMC6933872/fullTextXML` returned 500 in
#    four separate runs (R15/R16/R17/R18) — a persistent upstream 500, not jitter. It is not a
#    harness defect: the deliverables declared it in their coverage line (R18: "R7 返回 500 …
#    未取回全文的原因类为「非 OA」"), i.e. the *facts* were handled while the *gate* could not
#    see the event. `fetch-failed` is therefore visible + printed but not a FAIL.
