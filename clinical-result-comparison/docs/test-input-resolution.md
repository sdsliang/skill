# 测试前 ID 解析与试验设计复核决策（2026-09-18）

用户确认：MCP 的试验设计相关字段基于登记平台；应重点与论文原文复核，有冲突时以原文为准，披露登记平台字段的不同表述。双盲与三盲/四盲可能涉及受盲角色口径，不应仅凭词面差异指控抽取错误。R14 已披露冲突，R22 漏披露，故尚不能宣称执行稳定。后续用户纠正后已落实仓库 Skill/prompt（见下），未发布。

## 运行规则落实（同日追加）

已修改 `references/input-contract.md`（权威规则）、`SKILL.md`、`references/input-and-extraction.md` 与系统提示词 v0.15：逐条核对盲法、随机化、对照设计、阳性/安慰剂对照；同研究/人群/阶段比较，原文优先并披露登记平台描述；角色或版本差异须有证据，原文缺失只标未确认。交付前核对正文、表格及图表标签。

本地验证：上游 `deps` unchanged；19 文件技能包重建与 `--check` 一致（86,621 B，SHA-256 `e91f474fa80b450b5b18b72acc93e9dbc12f0a7bab181a0ae77d70da994ef887`）；`git diff --check` 通过。未改冻结评分器或测试基线，未改 ID 运行契约、runner、TS 工具权限；无 Docker 构建。本地待发布，无新版在线 R 验证，不能宣称已修复线上表现。

发布授权后使用 `toolsmith-publish run --web` 验证：`24_1_39054491_1` 与 `24_1_42745778_1`（双盲/开放、单盲/开放），并补查 `24_1_42747328_1` 与 `24_1_42744897_1`（论文双盲 vs 登记四盲/三盲）。以当次真实回执和取得的原文为准，人工查冲突披露、角色/阶段区分、原文缺失说明及各展示位置一致性；不把静态契约或旧分数通过当作内容通过。


## 测试前 ID 解析

用户测试输入拿不到真实 `_id`，因此兼容仅在我们调用 TS 测试前由只读 SQL 完成，不加入 Skill，不改 runner 或项目工具权限。

- 明确 `_id` / `esids`：仅匹配 `_id`；明确 `extra_esid`：仅匹配 `extra_esid`；明确 PMID / `pm_id`：仅匹配 `pm_id`。
- 未指定字段的记录 ID：`(_id IN (...) OR extra_esid IN (...)) AND is_delete != '是'`。字段只允许上述白名单；值使用参数绑定或正确 SQL 转义，不插入用户提供的 SQL 片段。
- 返回 `_id`, `extra_esid`, `pm_id`，必要时带标题供核对。保留输入到命中记录的映射，按输入顺序汇总，按真实 `_id` 去重；一对多保留所有命中，不任意取首条。
- 仅将解析后的真实 `_id` 传给 TS 的 `esids`。未解析输入单列，不能悄悄丢弃后声称原始完整名单均已测试；部分输入测试要明确范围。
- 不猜 `nr_` 前缀，不把 PMID 塞入 `esids`。保留查询、返回、转换清单供复核。
- 此流程用于真实记录的测试输入准备；故意无效 ID 的拒绝场景（如冻结场景 B）保留原始输入，不提前过滤或修正。

## 本次证据

同一表、同一删除条件：154 个源 ID 按 `_id` 命中 85；按 PMID 命中 86。新增记录 `_id=nr_24_1_42680109_1`，`extra_esid=24_1_42680109_1`，PMID 42680109。前 68 条按 PMID 仍未返回，原因未定，不认定为同步延迟。

证据目录：`~/.local/state/toolsmith-runs/20260918-161344-pmid-full-probe/`：

- `pmid-debug-request.json` / `pmid-debug-response.json`：真实只读 SQL 请求和 86 条返回。
- `test-input-resolution.json`：基于上述既有 SQL 返回，再按 `_id/extra_esid` 精确相等核对出的转换清单（86 个真实 `_id`、68 个未解析输入）；不是新执行的双字段查询。

## A10 归因更正

撤回此前“上下文误抽”的归因。用户提供的 `24_1_39054491_1` 原始 ES 中 `research_design.evidence_labels.blind` 为 Double-Blind / 双盲，与原文一致；TS MCP 返回开放。用户进一步确认 MCP 设计字段取自登记平台，因此应区分登记平台与论文口径。未修改数据，也未向开发发送问题单。
